 class Solution {

public int [] sortArrayByParity(int[] A) {
    int[] output = new int[A.length];

int index = 0;

for (int i: A) if (i % 2 == 0) output[index++] = i;
for (int i: A) if (i % 2 == 1) output[index++] = i;


return output;
}
 }
